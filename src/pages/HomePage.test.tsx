import { render } from "../test-utils"
import HomePage from "./HomePage"

describe("Home Page Test", () => {
  it("see user 0 is in the context", () => {
    // render(<HomePage />)
    expect(true).toBe(true);
  })
})